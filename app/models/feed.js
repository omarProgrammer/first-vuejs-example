import axios from 'axios'
import CONFIG from './../config';

class Feed {
	constructor() {

	}

	/**
	* @description method to get  data with get http's method
	* @param url: the url
	* @param data the  data of the url request
	* @return @promise
	**/
	get(url, data) {

	}

}

export default new Feed();